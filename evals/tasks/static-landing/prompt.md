Build a single-page marketing site in index.html for a company called Acme.

It must include:
- a hero section with an <h1> that contains the text "Acme"
- a <nav> with at least 3 links
- a contact section containing a <form> with a name field, an email input (type="email"), and a submit button

Plain HTML and CSS only — no build step, no external dependencies.
